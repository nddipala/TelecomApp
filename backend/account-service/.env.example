# Port the Account Service listens on
PORT=4001

# Comma-separated list of allowed origins for CORS.
# Use your Vercel URL in production, e.g. https://your-app.vercel.app
# Leave unset to allow all origins (convenient for local development).
CORS_ORIGIN=
