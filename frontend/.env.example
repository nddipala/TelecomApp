# Base URLs for each backend microservice.
# For local development these point at the local Express servers.
# In production (Vercel), set these to your deployed Render/Railway URLs.

VITE_ACCOUNT_API_URL=http://localhost:4001
VITE_DEVICE_API_URL=http://localhost:4002
VITE_UPGRADE_API_URL=http://localhost:4003
