# Production build config (committed: these are non-secret routing values).
# On Vercel the API runs as serverless functions in this same project under /api,
# so every service shares the deployment origin with an /api base path.
VITE_ACCOUNT_API_URL=/api
VITE_DEVICE_API_URL=/api
VITE_UPGRADE_API_URL=/api
